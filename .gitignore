# Dependencies
node_modules/

# Build source (large, rebuild with scripts)
build/

# Runtime data
saves/
*.log

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
.vscode/
.idea/

# Built WASM artifacts — large, generated. Rebuild with scripts/build-*.sh.
# NOT committed, but DOES ship: package.json "files" includes src/, and npm
# ignores .gitignore when a files field is present. Build before publishing.
src/cheevos/wasm/
