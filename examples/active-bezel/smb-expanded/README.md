# Super Mario Expanded View Experiment

AB-R3 is deliberately an experiment: it proves that the package can own a
widescreen scene, place the original frame off-center, inspect live CIRAM/CHR,
and expose opt-in gameplay mutations. It does not claim that an emulator can
magically render arbitrary off-screen NES geometry without game-specific
reconstruction.
